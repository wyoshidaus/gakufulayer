[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "gakufulayer"
version = "0.1.0.dev0"
description = "Multilingual vocal score preparation with page-block PDF preprocessing"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pypdf>=5,<7"]

[project.optional-dependencies]
dev = ["pytest>=8,<10"]

[project.scripts]
gakufulayer = "gakufulayer.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
